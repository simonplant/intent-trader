# Intent Trader IAA - Minimal Dependencies
# Core dependencies for chat-native trading assistant

# Core Python packages (pinned to secure versions)
pydantic>=2.11.5
python-dotenv>=1.0.0
requests>=2.32.3  # CVE-2024-35195 fixed in 2.32.3
urllib3>=2.4.0    # Latest secure version
certifi>=2025.4.26  # Latest certificate bundle
aiohttp>=3.12.2   # Latest secure version

# Data processing (essential for trading)
pandas>=2.0.0
numpy>=1.24.0

# Optional: Web interface (if needed for development)
fastapi>=0.115.12  # Latest secure version
uvicorn>=0.34.2    # Latest secure version

# Development dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
black>=23.10.0
isort>=5.12.0 