# Intent Trader IAA - Minimal Dependencies
# Core dependencies for chat-native trading assistant

# Core Python packages
pydantic>=2.0.0
python-dotenv>=1.0.0
requests>=2.31.0
aiohttp>=3.9.0

# Data processing (essential for trading)
pandas>=2.0.0
numpy>=1.24.0

# Optional: Web interface (if needed for development)
fastapi>=0.104.0
uvicorn>=0.24.0

# Development dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
black>=23.10.0
isort>=5.12.0 